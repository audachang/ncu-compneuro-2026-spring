[pytest]
# 註冊自訂 marker，避免 PytestUnknownMarkWarning
markers =
    network: 需要連到外部 API 的 test（用 -m "not network" 可跳過）

# 預設參數：每次跑都顯示測試名與短的 traceback
addopts = -ra --tb=short

# 收集規則（這些其實是預設值，但寫出來方便學生看）
python_files   = test_*.py
python_classes = Test*
python_functions = test_*
