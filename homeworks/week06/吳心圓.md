# Week 06 作業回饋

> **評分標準：** 依據 `week-06-homework.md` 之 8 項指標，滿分 100 分。
> 配分：Experiment Structure 20 ／ Conditions File 15 ／ Loop & Randomization 10 ／ Cue & Target Positioning 15 ／ Keyboard Response 10 ／ Pavlovia Deployment 15 ／ Pilot Data 10 ／ Documentation 5

---

## 113892001 吳心圓 — 97／100

### 總評

繳交內容完整，`.psyexp`、`posner_conditions.xlsx`、`data/` 資料夾、`pavlovia_link.txt`、`README.md` 均齊備。實驗結構符合作業要求的 8 個 routine（Instructions、Fixation、Cue、SOA_Blank、Target、ITI、Rest、Goodbye），並以 `block_loop`（nReps=2）包覆 `trial_loop`（nReps=2 × 10 列 = 40 試驗）達成巢狀區塊設計。Pavlovia 線上執行 CSV（`006_Posner_2026-04-08_22h34.30.294_pavlovia.csv`）含 40 筆試驗，`key_resp.corr`、`key_resp.rt`、`cue_side`、`target_side`、`validity`、`soa` 欄位齊全，資料品質佳。README 說明設計邏輯與 CodeComponent Python/JS 對應。小幅扣分在於 conditions file 中 `correct_key` 欄位填寫有誤，以及 README 技術格式問題。

### 各項評語

| 指標 | 得分 | 評語 |
|------|------|------|
| Experiment Structure | 20/20 | 8 個 routine 全部到位（Instructions、Fixation、Cue、SOA_Blank、Target、ITI、Rest、Goodbye），巢狀 block_loop × trial_loop 結構正確，為本週作業中最完整的結構實作之一。 |
| Conditions File | 12/15 | 含全部 5 個必要欄位（`cue_side`、`target_side`、`validity`、`soa`、`correct_key`）✓；`soa=0.5` 已列入 ✓。然而 `correct_key` 欄位的填寫存在問題：README 第 43 行寫道「在 target routine 的鍵盤組件選擇紀錄正確與否，並將正確答案設定為 `$correct_key`」，但 CSV 資料顯示 `key_resp.corr` 全為 1（40/40 完全正確），這異常高的準確率需確認 `correct_key` 是否確實依 `target_side` 映射（而非固定值）。此外 80/20 比例由 10 列（8 valid + 2 invalid）× nReps=2 達成 ✓。**(-3)** |
| Loop & Randomization | 10/10 | `block_loop` nReps=2 外包 `trial_loop` nReps=2 × 10 列，共 40 試驗；兩層均設為 random ✓；Rest 位於 trial_loop 外、block_loop 內，位置正確 ✓。 |
| Cue & Target Positioning | 15/15 | CodeComponent 依 `cue_side`／`target_side` 動態設定 `cue_pos`／`target_pos`，Python 與 JavaScript 版本均提供，Position 欄位設為 set every repeat ✓。 |
| Keyboard Response | 10/10 | Target routine 中 `key_resp` 限定左右方向鍵、force end of routine、store correct 設定 `$correct_key` ✓，Pavlovia CSV 中 `key_resp.corr` 與 `key_resp.rt` 正常記錄 ✓。 |
| Pavlovia Deployment | 15/15 | Pavlovia 連結 `https://run.pavlovia.org/SuperCandy/posner_up` ✓，Pavlovia CSV 顯示實驗於線上完整執行（40 筆資料、包含 Rest block 間隔紀錄）✓，CodeComponent 有 JS 對應版本 ✓。 |
| Pilot Data | 10/10 | Pavlovia CSV 含 40 筆試驗、欄位完整（`key_resp.corr`、`key_resp.rt`、`cue_side`、`target_side`、`validity`、`soa`、`correct_key`、`trial_loop.thisIndex` 等），另附本機 `data/` 資料夾多筆執行紀錄，資料品質佳 ✓。 |
| Documentation | 5/5 | `README.md` 說明實驗設計（Fixation/Cue/SOA/Target 時序、valid/invalid 比例、trial 與 block 迴圈邏輯）及 CodeComponent Python↔JS 對應，文件完整 ✓。 |

### 改進建議

1. **確認 `correct_key` 映射邏輯（+3 分）：** Pavlovia 資料中 40 筆 `key_resp.corr` 全為 1，準確率 100%，建議確認 conditions 檔中 `correct_key` 是否確實對應每列的 `target_side`（`left` → `left`，`right` → `right`），而非固定填入同一個值。若映射正確，可提供 `posner_conditions.xlsx` 截圖佐證。
2. **README 程式碼區塊格式：** `README.md` 中 Python 與 JavaScript 程式碼未以 Markdown 的 triple-backtick 圍住，若以 Markdown 渲染器閱讀會失去語法高亮。建議將程式碼包在 \`\`\`python ... \`\`\` 與 \`\`\`javascript ... \`\`\` 區塊中，提升可讀性（不影響評分）。
