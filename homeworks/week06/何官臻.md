# Week 06 作業回饋（Posner Spatial Cueing Task — Builder + Pavlovia）

## 114825002 何官臻 — 92／100

### 總評

**Flow 結構完整度最高**，嚴格依照作業指定的 8 個 routine（Instructions → Fixation → Cue → SOA_Blank → Target → ITI → Rest → Goodbye）實作，並使用 nested loop（`block_loop` × `trial_loop`）實現「2 blocks × 20 trials + Rest」的區塊設計 ✓。Cue 與 Target routine 皆有 CodeComponent 控制位置，且已完成 Pavlovia 線上部署（`Pavlovia_data_*.csv` 即為線上執行產出，證明 JS 版本可實際運作）。扣分主要來自條件檔 `validity` 欄位的標籤錯誤，以及 `readme.md` 空白缺少文件說明。

### 各項評語

| 指標 | 得分 | 評語 |
|------|------|------|
| Experiment Structure | 20/20 | 8 個 routine 全部到位：Instructons、Fixation、Cue、SOA_Blank、Target、ITI、Rest、Goodbye，Flow 排序正確，為本週作業中最完整的結構實作。 |
| Conditions File | 12/15 | 已包含全部 5 個必要欄位 `cue_side, target_side, validity, soa, correct_key` ✓，8 valid + 2 invalid 比例正確 ✓。但 `validity` 欄位中「valid」的 8 列皆誤填為 `"validity"`（應為 `"valid"`），會造成後續資料分析依 validity 分組時出錯。**(-3)** |
| Loop & Randomization | 10/10 | `block_loop` nReps=2 外包 `trial_loop` nReps=2 × 10 列 = 40 試驗，兩層皆設為 random ✓，Rest 在 trial_loop 外、block_loop 內，位置正確。 |
| Cue & Target Positioning | 15/15 | Cue 與 Target routine 各自包含 CodeComponent 依 `$cue_side`／`$target_side` 設定位置；Polygon 與 Text 的 Position 欄位使用 set every repeat，設計正確。 |
| Keyboard Response | 10/10 | Target routine 中 `key_resp` 限定左右方向鍵、store correct、最大 1.5 秒 ✓，CSV 中 `key_resp.corr`／`key_resp.rt` 正確記錄。 |
| Pavlovia Deployment | 15/15 | 已提供連結 `https://pavlovia.org/ireneho3507/posner_task`，並附上實際線上執行的 `Pavlovia_data_week06_hw_2026-04-07_23h58.25.416.csv`，證明 JS 版本可正常運作。 |
| Pilot Data | 10/10 | Pavlovia CSV 欄位齊全（含 `key_resp.corr`、`key_resp.rt`、`cue_side`、`target_side`、`trial_loop.thisN` 等），另附本機執行的 `local_run_data` 資料夾作為備份，資料品質佳。 |
| Documentation | 0/5 | ⚠️ `readme.md` 為**空檔案**（0 行），未提供設計說明或 Code Component 用途描述，不符合作業「Brief description explaining your design choices」要求。**(-5)** |

### 改進建議

1. **修正 `validity` 欄位標籤（+3 分）：** 將 8 列有效試驗的 `validity` 從 `"validity"` 改為 `"valid"`，否則統計時 `df[df.validity=="valid"]` 將回傳 0 筆。
2. **補寫 README（+5 分）：** 說明：(a) 為何選擇 nested block_loop / trial_loop 結構；(b) Cue 與 Target 兩處 CodeComponent 的 Python↔JS 對應；(c) CSV 欄位含意。完成後可拿回全部 5 分。
