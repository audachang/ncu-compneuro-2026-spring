# Week 02 Homework Feedback

## 113825002 (呂杰驛)
### Q1: Data Types
- **優點**：有正確轉換 `tr` 與 `te` 的資料型態，並在印出時使用 `type()` 驗證。使用 f-string 搭配 `:.2f` 達成格式化輸出。
- **建議**：`total_time` 的計算公式有誤，誤將 `num_volumes` (180) 乘上 360，而非乘上 `TR` (2.0)，導致計算出 64800 秒。未來在帶入變數進行運算時，建議直接使用變數名稱（如 `num_volumes * f_tr`），可減少帶入常數錯誤的風險。

### Q2: List Slicing
- **優點**：Slicing 語法使用正確，充分理解 `start:stop:step` 的結構，並在程式碼中補上詳細的註解說明 `[::-1]` 的機制。
- **建議**：無。此題的操作皆順利完成。

### Q3: Loop + Conditional
- **優點**：有獨立考慮輸入為 `0` 的邊緣情況（edge case），並成功利用 `while` 迴圈與 `.join()` 組合並反轉餘數。
- **建議**：無。邏輯清晰且運作正確。

### Q4: Spike Train 分析器
- **優點**：利用推導列表末端增加元素（如 `[0]` 和 `[1]`）來解決由於資料末端未引發條件判斷所造成的遺漏，為有巧思的邊界條件處理策略。有成功找出長度為 7 的兩段 ISI。
- **建議**：程式中利用 `ISI_start_index + 1` 取代原本的 index，雖然符合直覺上第一、第二個點的數數邏輯，但若依循 python 0-indexed 的邏輯，建議在輸出時維持並標註原本是以 0 為起始的 index，以利後續檢驗與核對原陣列。

---

## 113892001 (吳心圓)
### Q1: Data Types
- **優點**：正確完成各變數資料型態的轉換與確認，並於 `total_scan_time` 使用 `num_volumes * tr_float`，邏輯正確。
- **建議**：無。輸出摘要精確無誤。

### Q2: List Slicing
- **優點**：正確使用切片完成各項任務。
- **建議**：針對提問「步驟三四五彼此之間是無關的，並沒有步驟的先後順序，是這樣嗎？」，是的，題目用意為分別針對同一組 `eeg_signal` 訊號做不同切片操作的單獨練習，使用原始 `eeg_signal` 獨立作答是正確的。

### Q3: Loop + Conditional
- **優點**：具備針對 `number == 0` 的獨立處理，透過 `while` 逐漸將數字整除為 0，並將字串合併反轉，操作正確。
- **建議**：無。程式無誤。

### Q4: Spike Train 分析器
- **優點**：順利計算正確的 Firing Rate 與 Burst 的起點和長度。
- **建議**：
  1. 在計算 Burst 結束時間（`end_ms`）的輸出時，將結果加上了 `總毫秒長`（500ms），導致印出如 `(20-540 ms)` 的錯誤範圍。此處可檢查運算式，移除不必要的常數加總。
  2. 在計算最長 ISI 時間長度時，誤將 `max_isi_length` 乘上 `總毫秒長`（500 ms），導致得到 3500 ms 的錯誤結果；應乘上單個時間點的長度（10 ms）。
  3. 目前邏輯在遇到相同的最大長度 ISI 時未一併保留（迴圈內缺少了當 `current_isi_length == max_isi_length` 時的紀錄機制），造成只偵測到第一段 70 ms 的 ISI 失真，而遺漏了第二段相同長度的波段。可考慮改用清單儲存包含多段 ISI 索引的資訊。

---

## 114825002 (何官臻)
### Q1: Data Types
- **優點**：使用條件表達式（`if is_resting_state == True`）靈活處理字串輸出，並正確運算出 `total_time` 及分鐘數。
- **建議**：條件表達式中的 `is_resting_state == True` 可直接簡化寫為 `if is_resting_state:`，在 Python 語法中更為簡潔，是較為常見的寫法。

### Q2: List Slicing
- **優點**：切片參數設定精準，針對五項任務都有提供正確的 slicing 語法。
- **建議**：無。正確達成任務。

### Q3: Loop + Conditional
- **優點**：運算轉換邏輯正確，且在迴圈中透過 `str(remainder) + binary_str` 直接完成字串向左累積，免去後續需再次反轉列表的步驟，寫法精簡。
- **建議**：無。表現穩定。

### Q4: Spike Train 分析器
- **優點**：使用 `has_started` 變數作為「開關」來嚴謹判斷是否位處於兩個 spike 之間的思路邏輯清楚，明確避免未被 spike 包覆的結尾 0 被算入 ISI 中。
- **建議**：針對 Burst 的紀錄，在迴圈外亦有處理可能未被存入的最後段 Burst（`if current_burst_len >= 3`），有考量到 edge case，完整度高。ISI 演算法方面，目前僅透過 `current_isi_count > max_isi_count` 更新最大值，未考慮在 `==` 時記錄平手長度，會錯失第二段相同長度之 ISI。未來可考慮改為列表來同時儲存多個相同長度的潛在變量點。

---

## 比較總結表 (Comparative Summary Table)

| 學生姓名        | 呂杰驛 (113825002)             | 吳心圆 (113892001)               | 何官臻 (114825002)                 |
| --------------- | ------------------------------ | -------------------------------- | ---------------------------------- |
| Q1 資料型態     | 公式帶入常數誤植為 360         | 操作與相對數值計算正確           | 利用條件表達式達成動態字串計算正確 |
| Q2 List Slicing | 正確且帶有豐富測試註解         | 正確                             | 正確                               |
| Q3 轉二進位     | 正確處理 edge case             | 正確處理 edge case               | 字串向左累積前置寫法精簡           |
| Q4 Spike Train  | 推導清單末端元素作為防呆機制佳 | ISI 的單位放大計算有誤差，需校正 | 導入布林值作為條件開關防範邊界溢算 |
